<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.Y3.AnalyticsTeam.CT.Cancer.DAO.RecApiDAO">

	<!--根据需求调整缓存参数：-->
	<cache type="org.mybatis.caches.ehcache.EhcacheCache">
		<property name="timeToIdleSeconds" value="300"/>
		<property name="timeToLiveSeconds" value="900"/>
		<!-- 同ehcache参数maxElementsInMemory -->
		<property name="maxEntriesLocalHeap" value="1000"/>
		<!-- 同ehcache参数maxElementsOnDisk -->
		<property name="maxEntriesLocalDisk" value="10000000"/>
		<property name="memoryStoreEvictionPolicy" value="LRU"/>
	</cache>

	<insert id="insertRecApiResponse" parameterType="CtRecApiResponse">
		INSERT INTO CT_REC_API_RESPONSE(RECID,RESPONSE)
		values(#{recid,jdbcType=VARCHAR},#{response,jdbcType=VARCHAR})
	</insert>

	<insert id="insertRec4HLT" parameterType="CtRec4HLT">
		INSERT INTO CT_REC_API_HPBLIVE(RESPONSE_BATCH,DEVICE_NAME,DEVICE_BATTERYLEVEL,TEMPERATURE,UNIT,LOCATIONGUID,CREATETIME)
		values(#{response_batch,jdbcType=VARCHAR},#{device_name,jdbcType=VARCHAR},#{device_batterylevel,jdbcType=VARCHAR},#{temperature,jdbcType=DECIMAL},#{unit,jdbcType=VARCHAR},#{locationguid,jdbcType=VARCHAR},#{createtime,jdbcType=VARCHAR})
	</insert>

</mapper>
