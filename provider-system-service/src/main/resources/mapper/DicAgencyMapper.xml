<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.Y3.AnalyticsTeam.CT.provider.system.DAO.DicAgencyDAO">

	<!--根据需求调整缓存参数：-->
	<cache type="org.mybatis.caches.ehcache.EhcacheCache">
		<property name="timeToIdleSeconds" value="300"/>
		<property name="timeToLiveSeconds" value="900"/>
		<!-- 同ehcache参数maxElementsInMemory -->
		<property name="maxEntriesLocalHeap" value="1000"/>
		<!-- 同ehcache参数maxElementsOnDisk -->
		<property name="maxEntriesLocalDisk" value="10000000"/>
		<property name="memoryStoreEvictionPolicy" value="LRU"/>
	</cache>

	<select id="queryAgencyEnum" resultType="DicAgencyPO" >
		select * from
		( select rownum rn,t.* from (select * from CT_DIC_AGENCY order by code) t
		<choose>
			<when test="endindex>0">
				where rownum &lt; #{endindex} ) where rn &gt; #{startindex}
			</when>
			<otherwise>
				)
			</otherwise>
		</choose>
	</select>

</mapper>
